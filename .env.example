# Shared secret used by Val Town -> Vercel API
CRON_SECRET=replace_with_long_random_secret
# Optional: custom secret for encrypting one-time verification challenge tokens
# (fallback is CRON_SECRET if not set)
DEVICE_FLOW_SECRET=

# GitHub credentials used only at runtime inside Vercel
GITHUB_USERNAME=your_github_username
GITHUB_PASSWORD=your_github_password

# Required: GitHub authenticator app setup key (base32), used to generate TOTP automatically
GITHUB_OTP_SECRET=your_base32_totp_secret

# Optional (recommended): set your own ClawCloud data center sign-in URL
# Germany example:
CLAW_SIGNIN_URL=https://eu-central-1.run.claw.cloud/signin

# Optional alternative to CLAW_SIGNIN_URL:
# Set only the data center name and code will build https://<dc>.run.claw.cloud/signin
# CLAW_DATACENTER=eu-central-1
